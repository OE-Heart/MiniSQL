

```sql
//创建表语句
create table table1 (
	ID int,
    name varchar(50),
    assests float,
    primary key(ID)
);

//重复创建表，报错
create table table1 (
	ID int,
    name varchar(50),
    assests double,
    primary key()
);

//插入数据
insert into table1 values(1,'123',15.56);
insert into table1 values(2,'123',15.56);
insert into table1 values(3,'abc',15.56);
insert into table1 values(4,'abc',20);
insert into table1 values(5,'def',15.56);
insert into table1 values(6,'def',20);

//测试对主键插入重复数据，报错
insert into table1 values(1,'456',15.56);

//查询主键数据，只获得一条
select * from table1 where(ID=1);

//删除记录
select * from table1;
delete from table1 where (id<=2);
select * from table1;//比较前后结果

//查询非主键数据，返回多条记录
select name from table1 where(assests=15.56);

//测试范围查询
select * from table1 where (id<3);

//测试不同格式的指令，这我没好好看，根据结果写吧
select * from table1 where id>2;
select * from table1 where (id>=2);
select * from table1 where (id>=2);
select * from table1 where (id<=3);
select * from table1 where id=3;
select * from table1 where (id<>4);

//创建索引
create index iname on table1(ID);

//测试建立索引名重复的索引，报错
create index iname on table1(ID);

//测试在非unique的属性上建立索引,报错
create index iname on table1(name);

//删除索引
drop index iname;

//删除表语句
drop table table1;

//测试从文件读入指令，压力测试
//建立一张新表，插入10000条数据，进行select操作，进行有无index的运行速度比较
execfile insert10000.sql;
select * from insert10000 
	//记录时间
create index idindex on insert100(id);
select * from insert10000 
	//记录时间

//退出，测试文件保存
quit;

//再次打开，直接对insert10000进行select，正确
select * from insert10000; 
```





